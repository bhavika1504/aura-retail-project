services:
  - type: web
    name: aura-retail-os
    runtime: python
    rootDir: aura_retail_os
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
